import { CreditaccpipePipe } from './creditaccpipe.pipe';

describe('CreditaccpipePipe', () => {
  it('create an instance', () => {
    const pipe = new CreditaccpipePipe();
    expect(pipe).toBeTruthy();
  });
});
