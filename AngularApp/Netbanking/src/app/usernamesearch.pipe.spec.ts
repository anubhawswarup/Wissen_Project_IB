import { UsernamesearchPipe } from './usernamesearch.pipe';

describe('UsernamesearchPipe', () => {
  it('create an instance', () => {
    const pipe = new UsernamesearchPipe();
    expect(pipe).toBeTruthy();
  });
});
