import { DebitaccpipePipe } from './debitaccpipe.pipe';

describe('DebitaccpipePipe', () => {
  it('create an instance', () => {
    const pipe = new DebitaccpipePipe();
    expect(pipe).toBeTruthy();
  });
});
