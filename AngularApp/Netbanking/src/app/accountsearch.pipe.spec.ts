import { AccountsearchPipe } from './accountsearch.pipe';

describe('AccountsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new AccountsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
