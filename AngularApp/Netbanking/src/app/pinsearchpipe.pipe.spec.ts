import { PinsearchpipePipe } from './pinsearchpipe.pipe';

describe('PinsearchpipePipe', () => {
  it('create an instance', () => {
    const pipe = new PinsearchpipePipe();
    expect(pipe).toBeTruthy();
  });
});
